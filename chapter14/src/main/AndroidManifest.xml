<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chapter14">
    <!-- 互联网 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 定位 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- 存储卡读写 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!-- 下载时不提示通知栏 -->
    <uses-permission android:name="android.permission.DOWNLOAD_WITHOUT_NOTIFICATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".JsonConvertActivity" />
        <activity android:name=".HttpGetActivity" />
        <activity android:name=".HttpPostActivity" />
        <activity android:name=".DownloadApkActivity" />
        <activity android:name=".DownloadImageActivity" />
        <activity android:name=".HttpUploadActivity" />
        <activity android:name=".HttpImageActivity" />
        <activity android:name=".GlideCacheActivity" />
        <activity android:name=".GlideSimpleActivity" />
        <activity
            android:name=".GuessLikeActivity"
            android:theme="@style/AppCompatTheme" />
        <activity
            android:name=".PhotoDetailActivity"
            android:theme="@style/AppCompatTheme" />
    </application>

</manifest>